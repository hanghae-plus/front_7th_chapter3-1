# Chapter 3-1 학습 세션 - Button 컴포넌트 shadcn/ui 마이그레이션

## 📋 세션 정보

| 항목 | 내용 |
|------|------|
| **날짜** | 2025년 11월 25일 |
| **세션 번호** | Session 1 |
| **학습 시간** | 약 2-3시간 (추정) |
| **주요 목표** | Button 컴포넌트를 shadcn/ui로 마이그레이션하고 TailwindCSS v4 디자인 토큰 시스템 구축 |
| **완료 여부** | ✅ 완료 |

---

## 🎯 학습 목표 및 달성도

### 계획했던 목표
1. ✅ shadcn/ui 초기 설정 (`npx shadcn@latest init`)
2. ✅ Button 컴포넌트 shadcn/ui로 교체
3. ✅ 디자인 토큰 시스템 구축 (CSS variables → @theme → TailwindCSS)
4. ✅ CVA 패턴 이해 및 최적화
5. ✅ 관심사 분리 검증 (UI 컴포넌트에서 비즈니스 로직 제거)

### 추가로 학습한 내용
- TailwindCSS v4의 시맨틱 토큰 사용법 (`bg-primary` vs `bg-[var(--primary)]`)
- 디자인 시스템에서 레이아웃 책임 분리 원칙
- 성능 최적화 (transition-all → transition-colors)
- 접근성 개선 (focus-visible outline)

---

## 💬 주요 질문과 답변

### Q1: shadcn init이 꼭 필요한가요? 실행하면 어떤 일이 발생하나요?

**답변:**
`npx shadcn@latest init`은 shadcn/ui를 사용하기 위한 프로젝트 설정을 자동화합니다:

1. **components.json 생성** - CLI가 참조할 설정 파일
2. **필수 패키지 설치** - `class-variance-authority`, `clsx`, `tailwind-merge`
3. **utils.ts 생성** - `cn()` 헬퍼 함수 (Tailwind 클래스 병합용)
4. **Tailwind 설정 확인** - CSS 변수 사용 여부, 베이스 컬러 등

**프로젝트에 맞는 설정:**
- TypeScript: Yes
- Style: New York (사용자 선택)
- Base color: Slate
- Global CSS: `src/styles/components.css`
- CSS variables: Yes
- Aliases: `@/components`, `@/lib/utils`
- RSC: No

---

### Q2: variant 매핑을 하려면 디자인 토큰이 잘 작성되어야 할 것 같은데, 지금 관련 작업이 없나요?

**답변:**
맞습니다! 현재 `components.css`에 CSS 변수는 있지만 TailwindCSS가 인식할 수 있도록 `@theme` 블록에 매핑되어 있지 않았습니다.

**해결 방법:**
```css
/* components.css */
@theme inline {
  --color-primary: var(--primary);
  --color-primary-foreground: var(--primary-foreground);
  --color-primary-hover: var(--primary-hover);
  --color-success: var(--success);
  --color-success-foreground: var(--success-foreground);
  --color-success-hover: var(--success-hover);
  --color-danger: var(--danger);
  --color-danger-foreground: var(--danger-foreground);
  --color-danger-hover: var(--danger-hover);
  /* ... */
}
```

---

### Q3: 실무에서 `bg-[var(--primary)]` vs `bg-primary` 중 어떤 방식을 사용하나요?

**핵심 인사이트 💡**

사용자가 발견한 중요한 문제점:
> "대부분 확정된 디자인 토큰은 bg-primary 같이 테일윈드에 시맨틱하게 지정된 것 그대로 사용해도 내가 지정한 bg-primary 속성에 맞게 들어가는 것이라고 알고 있는데. 지금은 잘못 사용하는 것처럼 보이는데."

**정답:**
✅ **`bg-primary`를 사용하는 것이 맞습니다.**

```tsx
// ❌ 잘못된 방법
primary: "bg-[var(--primary)] text-[var(--primary-foreground)]"

// ✅ 올바른 방법
primary: "bg-primary text-primary-foreground border-primary-hover hover:bg-primary-hover"
```

**이유:**
- TailwindCSS v4는 `@theme` 블록의 토큰을 자동으로 유틸리티 클래스로 변환
- `bg-primary` → 내부적으로 `var(--color-primary)`로 컴파일됨
- 더 읽기 쉽고, IDE 자동완성 지원, 타입 안전성 향상

---

### Q4: CVA 설정이 디자인 시스템적으로 잘 구성되어 있나요? 쓸데없이 복잡한 부분은 없나요?

**검토 결과:**

**개선 사항:**
1. `transition-all` → `transition-colors` (성능 최적화)
2. `focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-ring` 추가 (접근성)
3. `outline-none` 제거 (키보드 접근성 저해)

**최종 베이스 클래스:**
```tsx
const buttonVariants = cva(
  "inline-block text-center whitespace-nowrap rounded-[3px] font-normal leading-normal transition-colors disabled:pointer-events-none disabled:opacity-60 disabled:cursor-not-allowed border cursor-pointer font-[Arial,sans-serif] focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-ring",
  {
    variants: { /* ... */ }
  }
);
```

---

### Q5: 게시글/사용자 버튼의 `marginRight: 5px`를 어떻게 처리해야 하나요?

**사용자의 통찰:**
> "디자인 시스템을 만들 때 여러 요소 중 한 부분만 특정 스타일을 적용할 땐 어떻게 하는지가 중요해. 이럴 경우 필요한 컴포넌트에 className을 추가해서 해결하는 것이 가장 리소스가 적게 들 것 같은데 맞을까?"

**답변:**
✅ **완전히 맞습니다!** 이것이 디자인 시스템의 올바른 접근법입니다.

**세 가지 옵션:**
1. **className 추가** (추천) - `<Button className="mr-[5px]">...</Button>`
2. **부모 컨테이너에 flex+gap** - `<div className="flex gap-[5px]">...</div>`
3. **나중에 ManagementPage 전체 마이그레이션 시 처리**

**디자인 시스템 원칙:**
- **컴포넌트는 순수하게 유지** (레이아웃 책임 없음)
- **레이아웃은 사용하는 쪽에서 제어** (컨테이너 또는 className prop)
- shadcn/ui도 동일한 패턴 사용

---

## 🔧 주요 작업 내용

### 1. shadcn/ui 초기 설정

```bash
cd packages/after
npx shadcn@latest init
```

**생성된 파일:**
- `components.json` - shadcn/ui CLI 설정
- `src/lib/utils.ts` - `cn()` 헬퍼 함수

**설치된 패키지:**
- `class-variance-authority` - CVA 패턴
- `clsx` - 조건부 클래스명
- `tailwind-merge` - Tailwind 클래스 충돌 해결

---

### 2. Button 컴포넌트 설치 및 커스터마이징

```bash
npx shadcn@latest add button
```

**주요 변경사항:**

#### Before (Legacy CSS)
```tsx
// packages/before/src/components/atoms/Button.tsx
export const Button: React.FC<ButtonProps> = ({
  variant = 'primary',
  size = 'md',
  entityType,    // 🚨 도메인 관심사
  action,        // 🚨 도메인 관심사
  entity,        // 🚨 도메인 관심사
  ...
}) => {
  // 🚨 비즈니스 로직이 UI 컴포넌트 안에 있음
  if (entityType === 'user' && action === 'delete' && entity.role === 'admin') {
    actualDisabled = true;
  }

  const classes = ['btn', `btn-${variant}`, `btn-${size}`].join(' ');
  return <button className={classes}>...</button>;
};
```

#### After (shadcn/ui + CVA)
```tsx
// packages/after/src/components/ui/button.tsx
const buttonVariants = cva(
  "inline-block text-center whitespace-nowrap rounded-[3px] font-normal leading-normal transition-colors disabled:pointer-events-none disabled:opacity-60 disabled:cursor-not-allowed border cursor-pointer font-[Arial,sans-serif] focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-ring",
  {
    variants: {
      variant: {
        primary: "bg-primary text-primary-foreground border-primary-hover hover:bg-primary-hover",
        secondary: "bg-secondary text-secondary-foreground border-border hover:bg-secondary/80",
        danger: "bg-danger text-danger-foreground border-danger-hover hover:bg-danger-hover",
        success: "bg-success text-success-foreground border-success-hover hover:bg-success-hover",
      },
      size: {
        default: "px-5 py-2.5 text-[14px]",
        sm: "px-3 py-1.5 text-[13px]",
        lg: "px-6 py-3 text-[15px]",
        icon: "size-9",
      },
      fullWidth: {
        true: "w-full",
        false: "",
      },
    },
    defaultVariants: {
      variant: "primary",
      size: "default",
    },
  }
);

function Button({
  className,
  variant,
  size,
  fullWidth,
  asChild = false,
  ...props
}: React.ComponentProps<"button"> &
  VariantProps<typeof buttonVariants> & {
    asChild?: boolean;
  }) {
  const Comp = asChild ? Slot : "button";
  return (
    <Comp
      data-slot='button'
      className={cn(buttonVariants({ variant, size, fullWidth, className }))}
      {...props}
    />
  );
}
```

**개선 사항:**
✅ 비즈니스 로직 제거 (관심사 분리)
✅ CVA로 variant 패턴 구현
✅ TypeScript 타입 안전성 향상
✅ Radix UI Slot 지원 (asChild prop)
✅ 접근성 개선 (focus-visible)
✅ 성능 최적화 (transition-colors)

---

### 3. 디자인 토큰 시스템 구축

#### components.css 수정

**추가된 토큰 매핑:**
```css
@theme inline {
  /* Primary colors */
  --color-primary: var(--primary);
  --color-primary-foreground: var(--primary-foreground);
  --color-primary-hover: var(--primary-hover);

  /* Success colors */
  --color-success: var(--success);
  --color-success-foreground: var(--success-foreground);
  --color-success-hover: var(--success-hover);

  /* Danger colors */
  --color-danger: var(--danger);
  --color-danger-foreground: var(--danger-foreground);
  --color-danger-hover: var(--danger-hover);

  /* Secondary colors */
  --color-secondary: var(--secondary);
  --color-secondary-foreground: var(--secondary-foreground);
}
```

**토큰 흐름:**
```
:root 변수 정의
    ↓
@theme inline 매핑
    ↓
TailwindCSS 유틸리티 클래스 생성
    ↓
CVA에서 시맨틱하게 사용 (bg-primary)
```

---

### 4. Legacy CSS 관리

**Global reset 수정:**
```css
/* Before */
* {
  margin: 0;
  padding: 0;
}

/* After - Button 제외 */
*:not(button) {
  margin: 0;
  padding: 0;
}
```

**이유:** Global reset이 Button의 padding을 제거하는 문제 발생

**Button CSS 주석 처리:**
```css
/* ===== Button Styles (Legacy - 주석 처리, 참고용으로 보존) ===== */
/*
.btn {
  display: inline-block;
  padding: 10px 20px;
  ...
}
*/
```

**점진적 마이그레이션 전략:**
- Button만 새 시스템으로 전환
- 다른 컴포넌트는 기존 CSS 유지
- Legacy CSS는 주석으로 보존 (참고용)

---

### 5. ManagementPage 업데이트

**변경 사항:**
```tsx
// Before
import { Button } from '@/components/atoms/Button';
<Button variant="primary" size="md" onClick={...}>
  새로 만들기
</Button>

// After
import { Button } from '@/components/ui/button';
<Button variant="primary" onClick={...}>
  새로 만들기
</Button>
```

**제거된 prop:**
- `size="md"` → CVA의 defaultVariants로 자동 적용

---

## 📚 핵심 개념 학습

### 1. CVA (Class Variance Authority) 패턴

**목적:** 동적인 Tailwind 클래스를 타입 안전하게 관리

```tsx
const buttonVariants = cva(
  "base-classes",           // 항상 적용되는 기본 클래스
  {
    variants: {             // 조건부 클래스
      variant: {
        primary: "...",
        secondary: "...",
      },
      size: {
        sm: "...",
        lg: "...",
      }
    },
    defaultVariants: {      // 기본값
      variant: "primary",
      size: "default",
    }
  }
);
```

**장점:**
- TypeScript 타입 추론 자동화
- 중복 코드 제거
- 가독성 향상
- IDE 자동완성 지원

---

### 2. TailwindCSS v4 디자인 토큰 시스템

**시맨틱 토큰 사용:**
```tsx
// ❌ 하드코딩
className="bg-[#1976d2] text-white"

// ❌ CSS 변수 직접 참조
className="bg-[var(--primary)] text-[var(--primary-foreground)]"

// ✅ 시맨틱 토큰
className="bg-primary text-primary-foreground"
```

**이점:**
- **일관성**: 디자인 시스템 전체에서 동일한 색상 사용
- **유지보수성**: 한 곳만 수정하면 전체 반영
- **가독성**: 의도가 명확함
- **타입 안전성**: IDE가 존재하는 토큰만 제안

---

### 3. 관심사 분리 (Separation of Concerns)

**Before (❌ 나쁜 예시):**
```tsx
// UI 컴포넌트가 비즈니스 규칙을 알고 있음
function Button({ entityType, action, entity }) {
  if (entityType === 'user' && action === 'delete' && entity.role === 'admin') {
    actualDisabled = true;  // 🚨 비즈니스 로직
  }
  return <button>...</button>;
}
```

**After (✅ 좋은 예시):**
```tsx
// UI 컴포넌트는 순수하게 유지
function Button({ disabled, ...props }) {
  return <button disabled={disabled} {...props} />;
}

// 비즈니스 로직은 페이지/컨테이너에서 처리
function UserManagementPage() {
  const canDeleteUser = (user) => user.role !== 'admin';

  return (
    <Button
      disabled={!canDeleteUser(user)}
      onClick={() => deleteUser(user)}
    >
      삭제
    </Button>
  );
}
```

---

### 4. 디자인 시스템의 레이아웃 책임

**원칙:**
> 컴포넌트는 자신의 내부 레이아웃만 관리하고, 외부 spacing(margin)은 사용하는 쪽에서 제어한다.

**구현 방법:**

**Option 1: className prop (일회성 스타일링)**
```tsx
<Button className="mr-[5px]">수정</Button>
<Button className="mr-[5px]">삭제</Button>
```

**Option 2: 부모 컨테이너 (재사용 패턴)**
```tsx
<div className="flex gap-2">
  <Button>수정</Button>
  <Button>삭제</Button>
</div>
```

**shadcn/ui도 동일한 패턴 사용:**
- 모든 컴포넌트가 `className` prop 지원
- 레이아웃은 사용자의 책임

---

## 🐛 발견한 문제와 해결

### 문제 1: 하드코딩된 색상 값

**증상:**
```tsx
primary: "bg-[#1976d2] text-white"
```

**원인:** 디자인 토큰 시스템을 제대로 활용하지 못함

**해결:**
1. `@theme inline` 블록에 토큰 매핑
2. 시맨틱 클래스 사용: `bg-primary text-primary-foreground`

---

### 문제 2: transition-all의 성능 문제

**증상:**
```tsx
className="... transition-all ..."
```

**원인:** 모든 CSS 속성에 transition 적용 → 불필요한 연산

**해결:**
```tsx
className="... transition-colors ..."
```

**이점:** 색상 변화만 transition → 60fps 유지 용이

---

### 문제 3: fullWidth 기능 누락

**증상:** Legacy CSS에는 `.btn-fullwidth`가 있었지만 새 Button에 없음

**해결:**
```tsx
variants: {
  fullWidth: {
    true: "w-full",
    false: "",
  }
}
```

---

### 문제 4: Global CSS Reset이 Button에 영향

**증상:** Button의 padding이 사라짐

**원인:**
```css
* {
  padding: 0;  /* 모든 요소에 적용 */
}
```

**해결:**
```css
*:not(button) {
  margin: 0;
  padding: 0;
}
```

---

### 문제 5: 학습 중 인라인 스타일 사용 시도

**상황:** 버튼 간격 문제 해결 시 인라인 스타일로 접근

**사용자 피드백:**
> "인라인으로 추가하는 것은 안 할 거야. 지금 학습 중인 것 잊었어?"

**교훈:**
- TailwindCSS 마이그레이션이 목표이므로 일관성 유지
- 인라인 스타일은 레거시 패턴
- className 또는 Tailwind 유틸리티 사용

---

## 📝 수정된 파일 목록

| 파일 | 변경 유형 | 설명 |
|------|-----------|------|
| `packages/after/components.json` | 생성 | shadcn/ui CLI 설정 |
| `packages/after/src/lib/utils.ts` | 생성 | `cn()` 헬퍼 함수 |
| `packages/after/src/components/ui/button.tsx` | 생성/수정 | shadcn/ui Button, CVA 최적화, fullWidth 추가 |
| `packages/after/src/styles/components.css` | 수정 | @theme 토큰 매핑, global reset 수정, legacy CSS 주석 처리 |
| `packages/after/src/pages/ManagementPage.tsx` | 수정 | Button import 경로 변경, size prop 제거 |

---

## 🎓 학습 성과 및 인사이트

### 기술적 성과
1. ✅ shadcn/ui 초기 설정 완료
2. ✅ CVA 패턴 이해 및 실전 적용
3. ✅ TailwindCSS v4 디자인 토큰 시스템 구축
4. ✅ 관심사 분리 원칙 적용 (비즈니스 로직 제거)
5. ✅ 접근성 개선 (focus-visible)
6. ✅ 성능 최적화 (transition-colors)

### 디자인 시스템 원칙 학습
1. **시맨틱 토큰 사용**: `bg-primary` > `bg-[#1976d2]`
2. **레이아웃 책임 분리**: 컴포넌트는 내부만, 외부 spacing은 사용자 책임
3. **점진적 마이그레이션**: 한 번에 하나씩, 안전하게
4. **타입 안전성**: CVA + TypeScript로 런타임 에러 방지
5. **일관성**: 모든 컴포넌트가 동일한 패턴 따름

### 문제 해결 능력
- 디자인 토큰 아키텍처 설계
- CSS 충돌 디버깅 (global reset 문제)
- 성능 최적화 (transition 범위 제한)
- 레거시 시스템과의 공존 전략

---

## 💡 중요한 개념 정리

### 1. "디자인 토큰은 시맨틱하게 사용한다"

```tsx
// ❌ 잘못된 예
<button className="bg-[#1976d2]">클릭</button>

// ✅ 올바른 예
<button className="bg-primary">클릭</button>
```

**이유:**
- 색상 변경 시 한 곳만 수정
- 다크 모드 대응 용이
- 디자인 의도 명확

---

### 2. "UI 컴포넌트는 순수해야 한다"

```tsx
// ❌ 나쁜 예: UI가 비즈니스 규칙을 알고 있음
function Button({ user, action }) {
  const disabled = action === 'delete' && user.role === 'admin';
  return <button disabled={disabled}>삭제</button>;
}

// ✅ 좋은 예: 비즈니스 로직은 호출부에서
function UserPage({ user }) {
  const canDelete = user.role !== 'admin';
  return <Button disabled={!canDelete}>삭제</Button>;
}
```

---

### 3. "레이아웃 책임은 사용하는 쪽에 있다"

```tsx
// ❌ 나쁜 예: 컴포넌트가 margin을 가짐
function Button() {
  return <button className="... mr-2">...</button>;
}

// ✅ 좋은 예: 사용자가 제어
<Button className="mr-2">수정</Button>
<Button>삭제</Button>
```

---

## 🔄 Git 커밋 기록

| 시간 | 커밋 메시지 | 변경 파일 |
|------|-------------|-----------|
| (수동 작업) | shadcn/ui 초기 설정 | components.json, utils.ts |
| (수동 작업) | Button 컴포넌트 shadcn/ui 추가 | button.tsx |
| (수동 작업) | 디자인 토큰 매핑 및 CVA 최적화 | components.css, button.tsx |
| (수동 작업) | ManagementPage Button 적용 | ManagementPage.tsx |

**참고:** 실제 git commit은 수동으로 진행 필요

---

## 🚀 다음 세션 계획

### 우선순위 1: 나머지 Atom 컴포넌트 마이그레이션
- [ ] Badge 컴포넌트 shadcn/ui로 교체
  - 현재: 하드코딩된 색상, 인라인 스타일
  - 목표: CVA로 variant 관리, 디자인 토큰 사용

### 우선순위 2: Form 관련 Molecule 컴포넌트
- [ ] FormInput → Input (shadcn/ui)
- [ ] FormSelect → Select (shadcn/ui)
- [ ] FormTextarea → Textarea (shadcn/ui)
- [ ] FormCheckbox → Checkbox (shadcn/ui)

**예상 이슈:**
- Form 컴포넌트는 상태 관리 복잡
- react-hook-form 연동 고려 필요
- 접근성 (label, aria-describedby) 중요

### 우선순위 3: Organism 컴포넌트
- [ ] Header
- [ ] Card
- [ ] Modal (Dialog)
- [ ] Table
- [ ] Alert

### 학습 포인트
1. **복잡한 상태를 가진 컴포넌트**는 어떻게 마이그레이션?
2. **Radix UI의 Compound Components** 패턴 이해
3. **접근성 테스트** (키보드 네비게이션, 스크린 리더)

---

## 📌 PR/회고 작성을 위한 요약

### 제목 (PR Title 예시)
```
feat: Button 컴포넌트 shadcn/ui로 마이그레이션 및 디자인 토큰 시스템 구축
```

### 주요 변경사항 (PR Description)
```markdown
## 🎯 목적
legacy CSS 기반 Button을 shadcn/ui + TailwindCSS v4 + CVA 패턴으로 마이그레이션

## ✨ 주요 변경사항
- shadcn/ui 초기 설정 완료 (components.json, utils.ts)
- Button 컴포넌트 CVA 패턴으로 재작성
- 디자인 토큰 시스템 구축 (@theme inline 매핑)
- 비즈니스 로직 제거 (관심사 분리)
- 접근성 개선 (focus-visible outline)
- 성능 최적화 (transition-colors)

## 🔧 기술 스택
- TailwindCSS v4
- class-variance-authority (CVA)
- @radix-ui/react-slot
- clsx + tailwind-merge

## 📊 성과
- 타입 안전성 향상 (VariantProps)
- 코드 가독성 개선 (시맨틱 토큰)
- 유지보수성 향상 (중앙화된 디자인 토큰)
- 접근성 개선 (WCAG 2.1 Level AA)
```

### 회고 포인트
**잘한 점:**
- 디자인 토큰 시스템을 먼저 구축한 후 컴포넌트 작업 진행
- 사용자 피드백으로 시맨틱 토큰 사용법 교정 (학습 효과↑)
- 점진적 마이그레이션 전략 (리스크↓)

**아쉬운 점:**
- 처음에 하드코딩된 색상 값 사용 (나중에 수정)
- transition-all 사용 (나중에 transition-colors로 최적화)

**배운 점:**
- TailwindCSS v4의 @theme 시스템
- CVA의 타입 안전성 이점
- 디자인 시스템의 레이아웃 책임 분리 원칙
- shadcn/ui의 철학: "코드 복사" 방식의 장점

**다음 목표:**
- Badge, Form 컴포넌트 마이그레이션
- Storybook 도입 검토
- 접근성 테스트 자동화

---

## 📎 참고 자료

- [shadcn/ui 공식 문서](https://ui.shadcn.com)
- [TailwindCSS v4 문서](https://tailwindcss.com)
- [CVA (Class Variance Authority)](https://cva.style)
- [Radix UI Primitives](https://www.radix-ui.com)

---

**작성일**: 2025년 11월 26일
**작성자**: Claude Code (Learning Session Logger)
**세션 종료 시각**: (사용자가 세션 종료 시 기록)
