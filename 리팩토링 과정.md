## 과제 Q&A

**Q) 과제 README의 그밖에 해보면 좋을 것들에 Figma 관련 항목들이 있는데, 실제로 연습해볼 수 있는 Figma 파일이나 링크가 따로 제공되나요?**

https://www.figma.com/community/file/1055785285964148921

- Figma 디자인 토큰 추출 연습용 파일
- Design to Code 플러그인 테스트용 디자인 파일
- Icon to SVG 변환 연습용 아이콘 라이브러리

**Q) 과제에서 before의 시각적 디자인을 그대로 유지해야 하나요, 아니면 shadcn/ui 기본 디자인을 사용하거나 개선해도 되나요? 다크모드 색상도 직접 설계해야 하나요?**

⇒ 원래는 before 시각적 디자인을 유지해보세요 라고 과제를 드릴 생각이었다. 

⇒ shadcn/ui 기본 디자인을 사용하시고 왠만큼 CSS 자체를 거의 건들지 않으셔도 괜찮게 과제를 작성했습니다.

⇒ 다크모드를 할 수 있다. 커스텀을 이렇게 하면 되는구나

**Q) 현재 테스트 코드는 네이티브 select 요소를 querySelector로 찾는데, shadcn Select는 button으로 구현되어 있어서 테스트가 실패합니다. 테스트를 통과시키려면 숨겨진 네이티브 select를 추가하고 visibility hidden 처리하는 방법밖에 생각나지 않는데, 테스트를 위해 이 코드를 추가하는게 맞나..? 라는 생각이 들기도 합니다.**

**테스트 코드를 수정해도 되는지, 그것이 안된다면 어떤 방식으로 접근하는게 좋을지 궁금합니다!**

항상 기준이 있어야. 기준이 된다는 의미는 변하지 않는다.

기획이 변하지 않는 데 코드를 수정한다.  = 리팩토링 =  테스트가 기준이 된다. 이때는 테스트가 변하지 않아야 되죠.

코드가 변하는데 코드가 변한걸로 인해서 명세가 바뀌었다. = 고도화 = 기술이 기준이 되어 테스트를 수정해야 겠죠.

ps. shadcn/ui에는 Native Select 기반의 컴포넌트도 있어요! 이걸 쓰시면 어떨까

https://ui.shadcn.com/docs/components/native-select

**Q) 레거시에서부터 도입하려니까 뭐부터 건드릴지 모르곘어요..!**

⇒ 해도 변하지 않는 것들을 하면 되겠죠? shadcn/ui 설치하고 tailwindCSS 설치해서 환경 구축

⇒ Button 받아보고, Alert 받아보고

⇒ Button만 한번 바꿔보면서

⇒ 테스트 코드 돌려보고, 테스트가 뻑나면? 테스트를 수정할지 코드를 수정할 지 고민 좀 해보고

**Q) 문제점 분석하는 게 재밌었는데 토큰화 기준 잡는 게 고민이 많이 됩니당. value? semantic? component?**

**일단 색상이 여러개 있다보니 어디부터 어디까지 토큰으로 빼야할지의 기준이 모호한 것 같습니다!**

color semantic

- error
- success
- warning
- green
- info

https://www.figma.com/design/PMpnWAAK1cKI0gbsuBQ2tJ/Wanted-Design-System--Community-?node-id=16306-184223&m=draw&t=AG3ymKG8BF1QFeQO-11

![image.png](attachment:cf3e8a79-f836-43fb-ab10-1dbcabb04590:image.png)

좋은이름은 창의적인게 아니라 팔레트 같은거에요. 그냥 참고해서 가져다가 쓰시는게 좋아요!

**component**

- 네이밍 컨벤션은 어떻게 지을꺼야만 고민하고 나머지는 AI에게 위임해도 좋아요.

**Q) 과제의 의도!!**

- 일단 최신 기술을 레거시 코드 환경에서 도입을 점진적으로 하는 경험을 간접적으로 해보기!
    - **빈번하지는 않는데 항상 곁에 있는 작업..**
    - 할줄 알아야 하는데 테스트 코드 처럼 해보면 별거 아닌데 그 한번 해봤다 말하기 애매한
- “설계” 챕터이다 보니까
    - 대놓고 안티패턴의 형태로 넣어드렸는데 UI와 비즈니스 로직이 왜 함께 하면 안되는가?
- 디자인 시스템이라고 하는 세계에서의 기술적 생태계를 이해했으면 좋겠다.
    - 생태계의 배경과 역사를 이해하고
    - 범주를 이해하고
    - 각 기술의 대표주자와 alternative들이 어떤 것들이 있고 왜(=차별점) 나왔나?

---

## 시작

### 빌드 및 실행(+패키지 설치)

```bash
# fork한 저장소를 clone한 다음에 진행 부탁드립니다.
$ git clone https://github.com/<GITHUB_ID>/front_7th_chapter3-1.git

# 가져온 저장소의 폴더로 이동한 다음
$ cd front_7th_chapter3-1

# pnpm을 먼저 설치해야 합니다.
$ npm install -g pnpm

# 패키지를 설치해주세요. (node_module 폴더 생성됨)
$ pnpm install

# storybook 도 같이 설치해주세요
$ pnpm create storybook@latest

# 개발서버 실행
$ pnpm run dev
```

현재 packages/after 와 packages/before 폴더 동일

packages/after 폴더에 발제 요구사항에 맞게 수정 해야함

tailwind v4 + shadcn 설치

```bash
root 경로
⭐ 1. pnpm을 먼저 설치해야 합니다.
$ npm install -g pnpm

⭐ 2. 패키지를 설치해주세요. (node_module 폴더 생성됨)
$ pnpm install

packages/after 경로
⭐ 3. 경로 이동
$ cd packages/after

~~# tailwind v4 설치
$ pnpm add -D tailwindcss@^4.1.17~~

⭐ 4. Tailwind v4 재설치 (바로 위의 것 안해도 됐었을 듯...아닌가?)
$ pnpm add -D tailwindcss @tailwindcss/vite

⭐ 5. vite.config.js 수정 (이게 제일 중요함)
반드시 아래와 같아야 Tailwind v4가 동작함.

import path from "path";
import tailwindcss from "@tailwindcss/vite"; <- 추가
import react from "@vitejs/plugin-react";
import { defineConfig } from "vite";

export default defineConfig({
  plugins: [react(), tailwindcss()],  <- 수정
  resolve: {
    alias: {
      "@": path.resolve(__dirname, "./src"),
    },
  },
});

⭐ 6. globals.css 추가 및 수정 (v4 방식)
Tailwind v4는 config가 없고 CSS가 설정 파일 역할을 함

@import "tailwindcss";
(또는 @import "tailwindcss/utilities";)

여기에 shadcn 디자인 토큰(HSL/OKLCH 변수)도 이 안에 넣어야 함.

⭐ 7. src/main.tsx(또는 index.tsx) 수정
이거 빠지면 shadcn 스타일은 절대 적용 안 됨
import "@/globals.css";

(# components.json 파일 삭제
shadcn init 은 프로젝트 루트에 `components.json` 파일이 이미 존재하면 절대 초기화를 진행하지 않습니다.
그래서 오류 메시지가 아래처럼 뜨는 겁니다:
A components.json file already exists at ...\packages\after
To start over, remove the components.json file and run init again.

즉, 해결 방법은 components.json 파일을 삭제하고 다시 init 하는 것뿐입니다.)

⭐ 8. shadcn 설치 (핵심)
Tailwind v4에서는 shadcn@latest 사용하면 100% 실패함
→ 반드시 canary 버전 사용해야 함.
→ 이때, globals.css에 자동으로 필요한 내용 작성됨

$ npx shadcn@canary init

React 19 경고 뜨면:
$ npx shadcn@canary init -force

(# tw-animate-css 설치 여부
globals.css 에 다음이 있음:
@import "tw-animate-css";

그런데 패키지를 설치했는지?
✔ 설치했어야 함
$ pnpm add tw-animate-css
설치 안 되어 있으면 빌드가 되더라도 스타일이 깨진다.)

⭐ 9. shadcn 컴포넌트 생성 (src/components/ui 폴더 생성 후 컴포넌트 생성됨)
# 1) Button
npx shadcn@canary add button

# 2) Input
npx shadcn@canary add input

# 3) Select
npx shadcn@canary add select

# 4) Form (react-hook-form + zod example 포함)
npx shadcn@canary add form

# 5) Card
npx shadcn@canary add card

# 6) Table (DataTable 템플릿도 필요하면 추가 옵션 선택)
npx shadcn@canary add table
```

- tailwind v4 + shadcn 설치하기 위한 뻘짓 (v3 설치 후 v4로 업데이트…)
    
    ```bash
    # tailwind v3 설치 (shadcn을 사용하기 위해서는 tailwind.config.ts가 필요한데 tailwind v4는 tailwind.config.ts를 무시하기 때문에)
    
    # packages/after 경로 이동
    $ cd packages/after
    
    # tailwind v3 설치
    $ pnpm add -D tailwindcss@^3.3.3 postcss autoprefixer
    
    # tailwind.config.js, postcss.config.js 파일 생성
    $ npx tailwindcss init -p
    
    # src/index.css(또는 globals.css) 파일 있어야 함
    # src/main.tsx 에 "import './globals.css'" 하기
    
    # tailwind.config.js 내용 수정
    /** @type {import('tailwindcss').Config} */
    export default {
      content: [
        "./index.html",
        "./src/**/*.{js,ts,jsx,tsx}",
      ],
      theme: {
        extend: {},
      },
      plugins: [],
    }
    
    # postcss.config.js 내용 수정
    export default {
      plugins: {
        tailwindcss: {},
        autoprefixer: {},
      },
    }
    
    # shadcn UI를 사용할 수 있는 상태로 초기화
    $ npx shadcn@latest init
    
    # shadcn 컴포넌트 생성 (src/components/ui 폴더 생성 후 컴포넌트 생성됨)
    $ npx shadcn@latest add button
    ```
    
    cusor에 요청
    
    @ManagementPage.tsx , components, styles 를 보고 shadcn 컴포넌트에 그대로 적용되게 하고싶어. 이러한 것을 리팩토링이라고 하나? 아무튼 현재 components에 기존에 개발한 컴포넌트들 외에 shadcn을 사용한 컴포넌트로 대체될 수 있도록 해줘
    
    > components/ui 폴더에 컴포넌트 생성
    > 
    > 
    > ManagementPage.tsx에 생성한 컴포넌트로 대체
    > 
    
    그런데 생각해 보니 shadcn 컴포넌트를 기반으로 수정한 것을 사용해야함
    
    ui에 생성한 컴포넌트 삭제 후 cli로 shadcn 컴포넌트 생성
    
    ```bash
       $ npx shadcn@latest add alert
       $ npx shadcn@latest add badge
       $ npx shadcn@latest add card
       $ npx shadcn@latest add dialog
       $ npx shadcn@latest add input
       $ npx shadcn@latest add label
       $ npx shadcn@latest add select
       $ npx shadcn@latest add table
       $ npx shadcn@latest add textarea
    ```
    
    Page.tsx에 컴포넌트 대체 한 후, before과 after 각 각 실행해서 비교해보기
    
    ```bash
    # 터미널1
    root$ pnpm run dev:before
    
    # 터미널2
    root$ pnpm run dev:after
    ```
    
    tailwind v4로 업데이트
    
    packages/after 실행 에러
    
    ```bash
    # v4로 업데이트
    $ pnpm add -D tailwindcss@^4.1.17
    
    # 실행
    $ pnpm run dev:after
    
    # 에러
    [plugin:vite:css] [postcss] It looks like you're trying to use `tailwindcss` directly as a PostCSS plugin. The PostCSS plugin has moved to a separate package, so to continue using Tailwind CSS with PostCSS you'll need to install `@tailwindcss/postcss` and update your PostCSS configuration.
    C:/projects_portfolio/hanghae_FE_7기/workspace/git/hanghae-plus_front_7th_chapter3-1/packages/after/src/globals.css:undefined:null
    
    # 에러 원인
    Tailwind v4에서는 기존 tailwindcss 패키지를 PostCSS 플러그인으로 직접 쓰면 안 되고, 새로 분리된 @tailwindcss/postcss 플러그인을 사용해야 합니다. 에러 메시지가 그 부분을 짚어준 거예요.
    
    # 해결 방법
    1. @tailwindcss/postcss가 packages/after에 설치되어 있는지 확인합니다. 루트에서 한 번 더 실행해 두면 안전
    		$ pnpm install @tailwindcss/postcss -D --filter @front_lite_chapter3-1/after
    
    2. tailwind v4에 맞게 환경 수정
    2-1. `packages/after/postcss.config.js`를 아래처럼 바꿉니다.
       export default {
         plugins: {
           '@tailwindcss/postcss': {},
         },
       }
       (v4에서는 이 플러그인이 autoprefixer까지 처리해 주므로 따로 추가하지 않아도 됩니다.)
       
    2-2. `tailwind.config.js`를 아래처럼 바꿉니다.
    	파일명을 `tailwind.config.ts`(또는 .js)로 변경하고 ESM으로 작성합니다.
    	import type { Config } from 'tailwindcss'
    
      const config: Config = {
        content: ['./src/**/*.{ts,tsx,js,jsx}'],
        theme: {
          extend: {
            // radius, colors 등 기존 확장 정의
          },
        },
        plugins: [],
      }
    
      export default config
      (플러그인이 필요하면 `import { typographyPlugin } from './src/theme/typography'`처럼 가져오
      고 `plugins: [typographyPlugin]`에 넣습니다.)
    
    2-3. `src/globals.css`를 v4 스타일로 전환
       - 가장 위에 `@config '../tailwind.config.ts';`를 추가해서 현재 CSS가 어떤 설정 파일을 참조하
    	   는지 선언합니다.
       - Tailwind 유틸/프리플라이트를 불러오려면 `@import 'tailwindcss';`를 사용합니다. 애니메이션
    	   플러그인 등도 CSS로 import합니다.
       - 기존 Root 변수나 베이스 레이어는 그대로 유지하되, 필요한 경우 다른 CSS 파일을 import해 
    	   구성합니다.
    
    	`globals.css`
    	 @config '../tailwind.config.ts';
       @import 'tailwindcss';
       @import './theme/primitive-colors.css';
    
       @layer base {
         :root {
           /* 변수 정의 */
         }
         body {
           background-color: hsl(var(--background));
           color: hsl(var(--foreground));
         }
       }
    		
    
    3. Vite dev 서버를 재시작하세요.  
       $ pnpm run dev:after
    
    이렇게 하면 정상적으로 빌드됩니다....
    하지만 shadcn 스타일이 안먹힘!!!!!!!! 진짜 너무 힘들다...왜 안될까?
    ```
    
    tailwind v4 + shadcn 스타일 적용되도록…방법
    
    ```bash
    packages/after 경로에서 진행
    
    ⭐ 1. Tailwind v4 삭제 & 재설치
    (지금은 v3 → v4 업그레이드라 config 구조가 꼬여 있는 상태임)
    $ pnpm remove tailwindcss postcss autoprefixer
    
    Tailwind v4는 postcss를 사용하지 않으므로 지우는 게 맞음.
    **그런데 에러가 나서 1번 skip 했음**
    
    ⭐ 2. Tailwind v4 재설치
    $ pnpm add -D tailwindcss @tailwindcss/vite
    
    ⭐ 3. vite.config.js 수정 (이게 제일 중요함)
    반드시 아래와 같아야 Tailwind v4가 동작함.
    
    import path from "path";
    import tailwindcss from "@tailwindcss/vite"; <- 추가
    import react from "@vitejs/plugin-react";
    import { defineConfig } from "vite";
    
    export default defineConfig({
      plugins: [react(), tailwindcss()],  <- 수정
      resolve: {
        alias: {
          "@": path.resolve(__dirname, "./src"),
        },
      },
    });
    
    ⭐ 4. globals.css 수정 (v4 방식)
    Tailwind v4는 config가 없고 CSS가 설정 파일 역할을 함
    
    @import "tailwindcss";
    (또는 @import "tailwindcss/utilities";)
    
    여기에 shadcn 디자인 토큰(HSL/OKLCH 변수)도 이 안에 넣어야 함.
    
    ⭐ 5. components.json 파일 삭제
    shadcn init 은 프로젝트 루트에 `components.json` 파일이 이미 존재하면 절대 초기화를 진행하지 않습니다.
    그래서 오류 메시지가 아래처럼 뜨는 겁니다:
    A components.json file already exists at ...\packages\after
    To start over, remove the components.json file and run init again.
    
    즉, 해결 방법은 components.json 파일을 삭제하고 다시 init 하는 것뿐입니다.
    
    ⭐ 6. shadcn 설치 (핵심)
    Tailwind v4에서는 shadcn@latest 사용하면 100% 실패함
    → 반드시 canary 버전 사용해야 함.
    
    $ npx shadcn@canary init
    
    React 19 경고 뜨면:
    $ npx shadcn@canary init -force
    
    이 과정에서:
    style: default
    tailwind: v4 구조 자동 감지
    색상 변수 생성됨
    
    **드디어 shadcn 스타일 적용됨!!!!**
    ```
    

### GIT Actions

초반 작업…[[4주차] GitHub Pages 배포 가이드 (Actions ver)](https://www.notion.so/4-GitHub-Pages-Actions-ver-2a72dc3ef51480fdb104d7748fc8b4aa?pvs=21) 

나는 `root/packages/after`와 `root/packages/before` 모두 배포하고 싶음

`{repo 이름}/` 링크에 들어갈 때, after 폴더가 나오게 하고 싶음 

> https://dev-learning1.github.io/hanghae-plus_front_7th_chapter3-1/
> 
> 
> (= https://dev-learning1.github.io/hanghae-plus_front_7th_chapter3-1/after)
> 
> [https://dev-learning1.github.io/hanghae-plus_front_7th_chapter3-1/before](https://dev-learning1.github.io/hanghae-plus_front_7th_chapter3-1/)
> 

1. .github/workflow/deploy.yml

```bash
name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install pnpm
        uses: pnpm/action-setup@v2
        with:
          version: latest

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "pnpm"

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      # before / after 자동 빌드
      - name: Build all packages
        env:
          GITHUB_PAGES: "true"
        run: |
          pnpm run build

          # dist 루트 생성
          rm -rf dist
          mkdir -p dist/before dist/after

          # 패키지별 결과 복사
          cp -R packages/before/dist/. dist/before/
          cp -R packages/after/dist/. dist/after/

          # 기본 접속("/") 시 after로 포워딩
          cp dist/after/index.html dist/index.html
          cp dist/index.html dist/404.html

          # 404 대응
          cp dist/before/index.html dist/before/404.html
          cp dist/after/index.html dist/after/404.html

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: "./dist"

      - name: Deploy to GitHub Pages
        uses: actions/deploy-pages@v4
```

1. packages/after와 packages/before 폴더 내, 빌드 경로 설정

```tsx
// vite.config.ts
import { defineConfig } from 'vitest/config'
import react from '@vitejs/plugin-react'
import tailwindcss from "@tailwindcss/vite";
import path from 'path'

const repoName = process.env.GITHUB_REPOSITORY?.split('/')[1] ?? 'hanghae-plus_front_7th_chapter3-1'
const isPages = process.env.GITHUB_PAGES === 'true'
const basePath = isPages ? `/${repoName}/after/` : '/'
// before 폴더에서는 `/${repoName}/before/` 로 수정

// https://vite.dev/config/
export default defineConfig({
  base: basePath,
  plugins: [react(), tailwindcss()],
  resolve: {
    alias: {
      "@": path.resolve(__dirname, "./src"),
    },
  },
  test: {
    globals: true,
    environment: 'jsdom',
    setupFiles: './src/test/setup.ts',
    css: true,
  },
})

```

1. packages/after와 packages/before 폴더 내, vite 버전 동일한지 확인 (v5로 맞추기. 더 안정)

```tsx
// package.json
"devDependencies": {
	"vite": "5.4.21"
}
```

1. package.json 수정 - preview 추가

```tsx
"scripts": {	
	"preview": "pnpm -r preview",
  "preview:after": "pnpm --filter @front_lite_chapter3-1/after preview",
  "preview:before": "pnpm --filter @front_lite_chapter3-1/before preview"
}
```

1. 로컬에서 빌드 환경 보기

```tsx
$ pnpm run build
$ pnpm run preview

port가 다른 두 링크가 나옴
로컬에서만 그렇게 나오는 것이고 배포환경에서는 원래 의도한 대로 잘 나옴
```

![image.png](attachment:f024f00f-7088-41a6-99b7-8563d6bc761d:image.png)

![image.png](attachment:be16f5c5-1c37-43c1-9d8c-0c40b7970775:image.png)

### 폴더 구조

packages/after를 root라고 생각하자

### UI 레이아웃 수정 및 컴포넌트 설계

1. cursor에 과제 요구사항에 맞게 수정해달라고 요청
    - 요청
        
        원래 packages/after와 packages/before 내용은 동일했어. packages/after 내용을 리팩토링하여 before보다 더 좋은 코드가 되도록 할거야. 무조건 아래 목표와 요구사항에 맞게 리팩토링 해야해.
        
        ## 4. 과제 목표 및 요구사항
        
        ### (1) Atomic Design Pattern - 이론과 현실의 괴리
        
        **현재 구조 (before):**
        
        ```
        components/
        ├── atoms/      # Button, Badge
        ├── molecules/  # FormInput, FormSelect
        └── organisms/  # Header, Card, Modal, Table
        
        ```
        
        **⚠️ 실무에서의 문제점:**
        
        1. **분류 기준이 모호함**
            - Card는 atom인가 molecule인가? 내용에 따라 달라짐
            - FormInput은 molecule이지만, 단독으로도 충분히 사용 가능
        2. **폴더 구조가 오히려 불편함**
            - 컴포넌트를 찾기 위해 3단계를 거쳐야 함
            - import 경로가 길어짐: `../../../components/atoms/Button`
            - 컴포넌트를 옮길 때마다 모든 import 수정 필요
        3. **개발 속도 저하**
            - "이게 atom인가 molecule인가?" 고민하는 시간 낭비
            - 팀원마다 분류 기준이 다를 수 있음
        
        **🎯 이번 과제의 목표:**
        
        - Atomic Design의 **개념 자체**를 이해하기 (컴포넌트 조합과 재사용성)
        - 하지만 **폴더 구조는 디자인 시스템과 개발구조가 다르다는 점** 이해하기
            - shadcn/ui도 `components/ui/` 단순 구조를 사용함을 주목
        
        ### (2) shadcn/ui 사용해보기
        
        **학습 내용:**
        
        - shadcn/ui의 설계 철학 이해
        - CLI를 통한 컴포넌트 추가
        
        - Radix UI 기반의 접근성 구현
        - 컴포넌트 커스터마이징
        
        **구현할 컴포넌트:**
        
        ```bash
        npx shadcn-ui@latest add button
        npx shadcn-ui@latest add input
        npx shadcn-ui@latest add select
        npx shadcn-ui@latest add form
        npx shadcn-ui@latest add card
        npx shadcn-ui@latest add table
        
        ```
        
        ### (3) TailwindCSS + CVA로 Variants 만들기
        
        **Before (문제):**
        
        ```tsx
        // 하드코딩된 스타일
        const getButtonStyle = (variant: string) => {
          if (variant === 'primary') return { backgroundColor: '#007bff', color: 'white' };
          if (variant === 'secondary') return { backgroundColor: '#6c757d', color: 'white' };
          // ...
        };
        
        ```
        
        **After (목표):**
        
        ```tsx
        import { cva, type VariantProps } from "class-variance-authority";
        
        const buttonVariants = cva(
          "inline-flex items-center justify-center rounded-md text-sm font-medium transition-colors",
          {
            variants: {
              variant: {
                primary: "bg-primary text-primary-foreground hover:bg-primary/90",
                secondary: "bg-secondary text-secondary-foreground hover:bg-secondary/80",
                destructive: "bg-destructive text-destructive-foreground hover:bg-destructive/90",
              },
              size: {
                sm: "h-9 px-3",
                md: "h-10 px-4 py-2",
                lg: "h-11 px-8",
              },
            },
            defaultVariants: {
              variant: "primary",
              size: "md",
            },
          }
        );
        
        ```
        
        ### (4) Storybook 사용해보기
        
        **Storybook 설정:**
        
        ```tsx
        // Button.stories.tsx
        import type { Meta, StoryObj } from '@storybook/react';
        import { Button } from './button';
        
        const meta = {
          title: 'UI/Button',
          component: Button,
          parameters: {
            layout: 'centered',
          },
          tags: ['autodocs'],
        } satisfies Meta<typeof Button>;
        
        export default meta;
        type Story = StoryObj<typeof meta>;
        
        export const Primary: Story = {
          args: {
            variant: 'primary',
            children: 'Button',
          },
        };
        
        ```
        
        **학습 내용:**
        
        - Storybook 설정 및 실행
        - Stories 작성 방법
        - Args와 Controls 활용
        - Accessibility addon 사용
        - 컴포넌트 문서 자동 생성
        
    - 결과
        
        ## 2 of 3 To-dos
        
        - [x]  ~~코드베이스 구조 파악 및 요구사항 분석~~
        - [x]  ~~packages/after 리팩토링~~
        - [ ]  스토리북 및 shadcn 구성 확인
        
        ## **변경 사항**
        
        - 엔티티·알림·다이얼로그 등 관리 페이지 전반을 기능 단위로 재구성하여 데이터 로딩, CRUD, 상태 액션, 통계 계산까지 한 곳에서 다루도록 구현했습니다. 도메인 전환 시 상태 초기화, 피드백 메시지, 다이얼로그 열림/닫힘을 모두 React 상태로 관리합니다. EntityForm, EntityTable, EntityStats, EntityToggle, EntityDialog를 조합해 Atomic 폴더를 대체하는 기능 중심 UI가 완성됐습니다.```1:205:packages/after/src/pages/ManagementPage.tsx
        - shadcn/ui 기반 입력·테이블·배지 변형을 사용하는 EntityTable/StatusBadge를 추가하여 검색, 카운트, 액션 버튼, 상태/역할 뱃지 표현을 분리했습니다. 포스트 전용 게시/보관/복원 액션과 공통 삭제·수정 버튼을 여기에서만 관리합니다.```1:200:packages/after/src/features/management/components/EntityTable.tsx
        - 새로운 AppHeader와 간결한 App 레이아웃을 추가해 배경/여백을 Tailwind 조합으로 처리하고, 더 이상 styles/components.css에 의존하지 않습니다.```1:19:packages/after/src/components/layout/AppHeader.tsx```1:13:packages/after/src/App.tsx
        - Vitest 시나리오는 Radix Select로 바뀐 폼에 맞춰 콤보박스 인터랙션을 검증하도록 갱신했습니다. CRUD 전 과정을 유지하면서도 접근성 쿼리를 사용하도록 개선했습니다.```11:90:packages/after/src/pages/**tests**/ManagementPage.test.tsx
        
        ## **테스트**
        
        - pnpm test:run (pass) – Vitest를 일회성 모드로 실행하여 주요 시나리오를 확인했습니다.
        
        ## **미해결/요청**
        
        - Storybook 설정을 위해 pnpm add -D storybook … 명령을 실행하려 했으나 환경에서 “User chose to skip the command”로 거부되었습니다. Storybook 의존성 설치와 .storybook 설정, Button.stories.tsx 추가가 남아 있습니다. 실행 권한을 주시거나 직접 pnpm dlx storybook@latest init/pnpm add -D storybook @storybook/react @storybook/react-vite @storybook/addon-essentials @storybook/addon-a11y @storybook/addon-interactions @storybook/test 등을 적용해 주시면 후속 구성이 가능합니다.
        - TODO 현황: 스토리북 및 shadcn 구성 확인 항목이 여전히 pending 상태입니다.
    - AS-IS VS TO-BE
        
        
        ![AS-IS](attachment:3f70195c-7dac-41d1-9b97-88b43a680dd2:image.png)
        
        AS-IS
        
        ![TO-BE](attachment:24827657-9b85-4086-a63c-55eac4deaa17:image.png)
        
        TO-BE
        
2. 기존 레이아웃과 비슷하게 다시 수정 및 페이지 네이션 추가
    - AS-IS VS TO-BE
        
        
        ![AS-IS](attachment:24827657-9b85-4086-a63c-55eac4deaa17:image.png)
        
        AS-IS
        
        ![TO-BE](attachment:9c34f238-0630-4a38-8fcc-98c1b3b521b4:image.png)
        
        TO-BE
        
3. 스타일 토큰화, cursor에 요청
    - 요청
    - 결과
        
        style/design-tokens.css 생성, globals.css 수정
        
        스타일 값은 design-tokens.css에서 설정
        
        globals.css에서는 tockens.css에서 설정한 것을 tailwind에 전달
        
    - AS-IS VS TO-BE
4. 다크 모드, cursor에 요청
    - 요청
        
        다크 모드 해달라고 부탁. 다크 모드 토글 컴포넌트 생성 요청. 토글 컴포넌트 위치 설정하여 다시 요청
        
    - 결과
        
        style/design-tokens.css, globals.css 에 .dark 토큰 생성 및 수정
        
        먼저 components/ui 에 토큰 기반 다크 모드 적용
        
        그 다음 features/components 및 page에 토큰 기반 다크 모드 적용
        
        직접 실행해 보니 테이블 및 모달창의 텍스트가 안보임. 다시 수정 요청하여 수정
        
    - TO-BE
        
        ![image.png](attachment:3e74c78a-af81-4f5e-834e-7df267757260:image.png)
        

## 여기까지 pr 중간 점검

### 과제 목표 및 사용할 도구

## **과제 목표**

**레거시 코드베이스를 현대적인 디자인 시스템으로 개편하는 실무 경험**

1. 정리되지 않은 레거시 코드의 문제점 식별 및 분석
2. TailwindCSS, shadcn/ui, CVA 등의 현대 도구 활용
3. 일관된 디자인 토큰과 컴포넌트 API 구축
4. UI와 비즈니스 로직이 적절한 분리된 리팩토링

---

## **Before 패키지 분석 후 After 패키지 개편**

### **개편 목표**

**디자인 시스템**

- TailwindCSS 기반 일관된 디자인 토큰 정의
- 하드코딩 제거, 재사용 가능한 스타일 시스템 구축
- dark mode, 반응형 등 확장 가능한 구조

**컴포넌트 아키텍처**

- UI 컴포넌트는 순수하게 UI만 담당
- 도메인 로직은 적절히 분리
- 일관된 컴포넌트 API 설계

### **사용할 도구**

**TailwindCSS 4.x**

- 디자인 토큰 기반 스타일링
- 유틸리티 클래스 활용
- dark mode, 반응형 내장 지원

**shadcn/ui**

- Radix UI 기반, 접근성 내장
- 복사 가능한 컴포넌트 (라이브러리가 아닌 소스코드)
- 자유로운 커스터마이징

**CVA (Class Variance Authority)**

- 선언적 variants 패턴
- 타입 안전한 스타일 조합
- 조건부 스타일링 처리

**React Hook Form + Zod**

- 선언적 폼 검증
- 타입 안전한 스키마
- 최소 리렌더링 최적화

### **준수된 항목**

- shadcn/ui 도입 및 CVA 변형 정의: button.tsx에서 shadcn 코드 스타일과 class-variance-authority를 활용해 일관된 variants/size/fullWidth 조합을 제공하고 있어 목표에 맞게 컴포넌트 API가 현대화되었습니다.
    
    ```bash
    // button.tsx
    const buttonVariants = cva(
      "inline-flex items-center justify-center whitespace-nowrap rounded-[3px] border text-[14px] leading-[1.5] font-normal transition-colors duration-150 ...",
      {
        variants: {
          variant: { default: primaryStyles, secondary: "...", ... },
          size: { sm: "px-3 py-1.5", md: "px-5 py-2.5", ... },
          fullWidth: { true: "w-full", false: "" },
        },
        defaultVariants: { variant: "default", size: "md", fullWidth: false },
      }
    )
    ```
    
- Tailwind v4 + 디자인 토큰 기반 스타일: 글로벌 CSS에서 Tailwind, tw-animate, 디자인 토큰을 결합하고 커스텀 variant/dark 토큰 매핑을 정의해 요구한 “토큰 기반 스타일 시스템”을 구축했습니다.
    
    ```bash
    // globals.css
    @import "tailwindcss";
    @import "tw-animate-css";
    @import "./styles/design-tokens.css";
    @theme inline {
      --radius-sm: calc(var(--radius) - 4px);
      ...
      --color-sidebar-ring: var(--sidebar-ring);
    }
    :root {
      --radius: var(--ds-radius-lg);
      --background: var(--ds-color-surface-base);
      ...
    }
    ```
    
- React Hook Form + Zod 폼 구축: EntityForm에서 사용자/게시글 스키마를 Zod로 선언하고 RHF useForm+resolver 조합으로 검증과 상태 관리를 통합해 목표 기술 스택을 충족합니다.
    
    ```bash
    // EntityForm.tsx
    const userSchema = z.object({
      username: z.string().min(3, "사용자명은 3자 이상이어야 합니다."),
      email: z.string().email("올바른 이메일 형식을 입력하세요."),
      ...
    });
    ...
    const form = useForm<UserFormValues>({
      resolver: zodResolver(userSchema),
      defaultValues,
    });
    ```
    
- PostManagement 페이지 마이그레이션: packages/after/src/pages/ManagementPage.tsx가 새 UI 컴포넌트(EntityStats, EntityTable, EntityDialog)와 서비스 계층을 사용하며, 전체 페이지가 after 패키지 내에서 동작하도록 이전되어 필수 요구사항 2번을 충족합니다.
    
    ```bash
    // ManagementPage.tsx
    export const ManagementPage = () => {
      const [entityType, setEntityType] = useState<EntityType>("post");
      ...
      return (
        <section className="min-h-screen bg-[color:var(--ds-color-neutral-100)] py-6">
          ...
          <EntityTable
            entityType={entityType}
            data={entities}
            onEdit={openEditDialog}
            onDelete={handleDelete}
            onStatusAction={handleStatusAction}
            onOpenCreateDialog={openCreateDialog}
          />
          ...
        </section>
      );
    };
    ```
    
- (심화) 다크 모드 토글까지 구현되어 ThemeToggle을 통해 토큰 전환이 가능하므로 선택 과제도 일부 수행되었습니다.

### **미준수 항목**

- Storybook 부재: 레포 전역에서 .storybook 디렉터리나 *.stories.* 파일이 전혀 없어(글롭 검색 결과 0건) “Storybook에 주요 컴포넌트 stories 작성” 요구사항이 충족되지 않았습니다. Storybook 셋업과 최소한 Button/Input/Table 등 핵심 컴포넌트 스토리가 필요합니다.
- README 문서화 미흡: README는 과제 설명/체크리스트 수준에 머물러 있고 실제 before/after 비교나 개선 내용 정리가 없습니다. 게다가 필수 구현 사항 체크박스도 여전히 미완료로 표시되어 요구된 문서화 기준을 충족하지 못합니다.

## 테스트 작업(src/pages/__tests__)

1. 기존 테스트 코드 실패
2. EntityForm.tsx 수정 → **HiddenNativeSelect 를 추가 했더니 기존 테스트 코드가 통과된 이유가 뭘까?**
    - **HiddenNativeSelect 구성**
        
        EntityForm.tsx 상단에 별도 컴포넌트로 정의했습니다. 필드 이름/값/옵션을 받아 <select>를 렌더링하고, onChange가 호출되면 RHF 필드 상태(field.onChange)가 그대로 업데이트됩니다.
        
        ```tsx
        const HiddenNativeSelect = ({ name, value, onChange, options, disabled }: HiddenNativeSelectProps) => (
          <select
            name={name}
            value={value ?? ""}
            onChange={(event) => onChange(event.target.value)}
            aria-hidden="true"
            tabIndex={-1}
            style={{ position: "absolute", width: 1, height: 1, opacity: 0, visibility: "hidden", pointerEvents: "none" }}
          >
            {options.map((option) => (
              <option key={option.value} value={option.value}>
                {option.label}
              </option>
            ))}
          </select>
        );
        ```
        
    - 숨겨진 네이티브 <select>를 추가했기 때문에 before 패키지에서 쓰던 테스트 코드를 그대로 after에 가져와도 동작합니다. 원본 테스트는 querySelector('select[name="role"]')나 user.selectOptions처럼 실제 <select> 엘리먼트를 전제로 작성돼 있었는데, 지금은 HiddenNativeSelect가 똑같은 DOM 구조를 만들어 주기 때문에 명세가 깨지지 않습니다. 결과적으로 “테스트도 변형돼야 한다”는 요구는 셀렉트가 버튼으로만 구현되어 있을 때의 이슈를 해소하자는 취지였고, 우리가 DOM에 네이티브 셀렉트를 복원한 순간 그 문제가 사라졌습니다. 따라서 기존 테스트를 그대로 유지해도 통과하며, 굳이 새 API에 맞춰 테스트를 바꿀 필요는 없습니다(현재 after 테스트에 적용한 fireEvent.change 방식도 동작하지만 선택 사항입니다).
3. 기존 테스트 코드 통과

## Storybook

```bash
# after경로 이동
$ packages/after

# storybook 도 같이 설치해주세요
$ pnpm dlx storybook@latest init

#필요한 패키지 설치
$ pnpm add -D storybook @storybook/react-vite @storybook/addon-essentials @storybook/addon-a11y @storybook/addon-interactions @storybook/test @storybook/react @storybook/types @storybook/addon-actions

# package.json 수정
	- 스토리북 버전 8로 해야함. 
	- @storybook/addon-actions은 9부터는 이 패키지가 사라져서 스토리북에서 에러나는 
		컴포넌트가 있음
	"scripts": {
		"storybook": "storybook dev -p 6006",
	  "build-storybook": "storybook build"
  },
  "dependencies": {
	  "@storybook/addon-a11y": "^8.6.14",
    "@storybook/addon-actions": "^8.6.14",
    "@storybook/addon-essentials": "^8.6.14",
    "@storybook/addon-interactions": "^8.6.14",
    "@storybook/react": "^8.6.14",
    "@storybook/react-vite": "^8.6.14",
    "@storybook/test": "^8.6.14",
    "@storybook/types": "^8.6.14",
  }
  
#  스토리북에 필요한 파일 생성 및 수정 (.storybook, 컴포넌트의 스토리북)
	`after/.storybook/main.ts`
		const config: StorybookConfig = {
		  stories: ["../src/**/*.mdx", "../src/**/*.stories.@(ts|tsx)"],
		  addons: [
		    "@storybook/addon-essentials",
		    "@storybook/addon-a11y",
		    "@storybook/addon-interactions",
		  ],
		  framework: {
		    name: "@storybook/react-vite",
		    options: {},
		  },
		  docs: {
		    autodocs: "tag",
		  },
		  viteFinal: async (baseConfig) => {
		    baseConfig.resolve ??= { alias: {} };
		    baseConfig.resolve.alias = {
		      ...(baseConfig.resolve.alias ?? {}),
		      "@": fileURLToPath(new URL("../src", import.meta.url)),
		    };
		    return baseConfig;
		  },
		}
	
	

# root 경로 이동
$ cd ../..

# storybook 재설치 및 실행
$ pnpm install 
$ pnpm --filter @front_lite_chapter3-1/after storybook

(# 정적 빌드
$ pnpm --filter @front_lite_chapter3-1/after build-storybook)

```

Q) `npm install --filter @front_lite_chapter3-1/after` 와 `pnpm --filter @front_lite_chapter3-1/after storybook -- --ci --smoke-test` 차이가 뭐야?

> 둘 다 --filter @front_lite_chapter3-1/after는 동일하게 after 패키지에만 작업을 적용한다는 뜻이지만, 실행 목적이 완전히 다릅니다.
> 
> 
> 
> - `pnpm install --filter @front_lite_chapter3-1/after`
> 
> 해당 패키지의 package.json에 적힌 의존성을 설치·동기화합니다. 즉, node_modules를 채우고 lockfile 반영을 확인하는 “설치” 명령입니다. 스토리북 실행과는 무관하며, 새 라이브러리를 추가했을 때 꼭 한 번 실행해야 합니다.
> 
> - `pnpm --filter @front_lite_chapter3-1/after storybook -- --ci --smoke-test`
> 
> 이미 설치된 의존성을 바탕으로 Storybook dev 서버를 CI 모드(화면 없이)로 구동해, 스토리들이 정상 빌드되는지 빠르게 체크합니다. 설치를 하지도 않고, 단순히 Storybook 빌드 파이프만 돌려보는 “테스트” 명령입니다.
> 

### storybook 배포(by git actions)

.github/workflow/deploy.yml 수정

```bash
name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install pnpm
        uses: pnpm/action-setup@v2
        with:
          version: latest

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "pnpm"

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      # before / after 자동 빌드 + storybook
      - name: Build all packages
        env:
          GITHUB_PAGES: "true"
        run: |
          pnpm run build
          pnpm --filter @front_lite_chapter3-1/after build-storybook <- 추가

          # dist 루트 생성
          rm -rf dist
          mkdir -p dist/before dist/after dist/storybook <- 수정

          # 패키지별 결과 복사
          cp -R packages/before/dist/. dist/before/
          cp -R packages/after/dist/. dist/after/
          cp -R packages/after/storybook-static/. dist/storybook/ <- 추가

          # 기본 접속("/") 시 after로 포워딩
          cp dist/after/index.html dist/index.html
          cp dist/index.html dist/404.html

          # 404 대응
          cp dist/before/index.html dist/before/404.html
          cp dist/after/index.html dist/after/404.html
          cp dist/storybook/index.html dist/storybook/404.html <- 추가

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: "./dist"

      - name: Deploy to GitHub Pages
        uses: actions/deploy-pages@v4

```

**빌드 에러 발생**

> 각 스토리(component.stories.tsx)에 기본 args를 정의하고 render에서는 해당 args를 사용해 내부 상태를 제어(또는 요구사항 만족)하도록 수정 필요
> 

`pnpm --filter @front_lite_chapter3-1/after tsc -b` 통과

> pnpm --filter @front_lite_chapter3-1/after … : 모노레포에서 after 패키지 하나만 대상으로 명령을 실행하라는 뜻입니다.
> 
> - tsc -b : TypeScript 프로젝트 레퍼런스(빌드 모드)로 컴파일만 수행하는 명령입니다. tsconfig.json에서 references로 연결된 tsconfig.app.json, tsconfig.node.json 등을 순서대로 빌드하고 타입 검사를 실행합니다. 실제 JS 번들 출력은 하지 않고, 타입 체크/emit 여부만 처리합니다.
> 
> 즉 이 커맨드는 after 패키지 내부의 TypeScript 프로젝트 전반에 대해 빠르게 타입 검사를 돌리는 용도로 사용합니다. preview나 Storybook 실행 전에 타입 오류가 없는지 확인할 때 유용합니다.
> 

**정적 빌드 확인**

단순히 pnpm run preview로 확인하려고 함 ← 잘못된 것

> pnpm run build → pnpm run preview → local:{port}/{repo}/story 하면 나오는 줄 알았음
> 
> 
> 안남옴. 
> 
> why?
> 
> pnpm run preview는 루트 dist를 Vite preview 서버로 띄우는 명령이라, 기본 라우팅이 dist/index.html → after 앱에 연결된 상태입니다. dist/after와 dist/storybook은 정적 자원으로만 존재하고, preview 서버는 별도의 프록시/리라이트 없이 단일 SPA로 인식하기 때문에 [http://localhost:4174/storybook](http://localhost:4174/after/storybook%EC%9D%84) 열어도 after 앱 라우터가 잡아 버립니다.
> 

npx serve나 pnpm dlx serve처럼 간단한 정적 서버를 띄우는 툴을 활용

> Storybook을 정적 파일로 빌드해두면, 단순히 그 폴더를 아무 정적 서버에 올리기만 하면 됨
> 

```bash
# `packages/after/storybook-static/`에 결과물 생성
pnpm --filter @front_lite_chapter3-1/after build-storybook

# 정적 서버로 호스팅 (로컬 확인)
$ cd packages/after/storybook-static
$ npx serve -p 6007 (또는 pnpm dlx serve -p 6007)

-> 브라우저에서 `http://localhost:6007` 접속하면 바로 Storybook 정적 사이트가 뜸
```

![image.png](attachment:f34097fd-7f92-4d17-8657-423c19591801:image.png)

![image.png](attachment:a10afcd4-f4b5-444f-adf5-a6cacc584e1a:image.png)